config:
  repositories:
    stable:
      name: stable
      url: "kubernetes-charts"
      username: "username"
      password: "password"
install:
  - helm:
      description: "Install MySQL"
      name: porter-ci-mysql
      chart: stable/mysql
      version: 0.10.2
